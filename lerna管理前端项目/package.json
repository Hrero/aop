{
  "name": "@hzrtest/root",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "git": "git add . && git commit -m '-' && git push origin master"
  },
  "publishConfig": {
    "access": "public"
  },
  "workspaces": [
    "packages/*"
  ]
}
